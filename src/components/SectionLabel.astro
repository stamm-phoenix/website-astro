---
interface Props {
  label: string;
  class?: string;
}

const { label, class: className = '' } = Astro.props;
---

<p class:list={["text-sm font-semibold uppercase tracking-[0.12em] text-[var(--color-brand-700)]", className]}>
  {label}
</p>
