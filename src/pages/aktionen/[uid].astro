// src/pages/aktionen/[uid].astro
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import aktionenData from '../../data/aktionen.json';

export async function getStaticPaths() {
  type LocalIcsEvent = { uid?: string; summary?: string; };
  return [{ params: { uid: 'test-event' }, props: { event: { uid: 'test-event', summary: 'Hardcoded Event' } as LocalIcsEvent } }];
}

const event = {} as any;
---

<BaseLayout title="Minimal Test" description="Minimal Test">
  <Header slot="header" />
</BaseLayout>